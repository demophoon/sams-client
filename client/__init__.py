import check
import cli
import config
import register

version = (0, 0, '0-alpha')
version_string = '.'.join([
    str(x) for x in version
])
full_version_string = "Sams Client Version %s" % version_string
